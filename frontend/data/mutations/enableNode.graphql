mutation enableNode($node: Uuid!) {
  enableNode(node: $node) {
    id
    name
    host
    username
    enabled
    reconciliationFailed
    port
    deployments {
      id
    }
  }
}
