mutation createDeployment($compose: Uuid!, $node: Uuid!) {
  createDeployment(compose: $compose, node: $node) {
    id
    compose {
      id
      name
    }
    node {
      id
      name
    }
  }
}
