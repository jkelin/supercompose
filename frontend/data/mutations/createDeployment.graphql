mutation createDeployment($compose: ID!, $node: ID!) {
  createDeployment(compose: $compose, node: $node) {
    id
    compose {
      id
      name
    }
    node {
      id
      name
    }
  }
}
