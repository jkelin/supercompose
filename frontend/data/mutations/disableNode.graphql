mutation disableNode($node: Uuid!) {
  disableNode(node: $node) {
    id
    name
    host
    username
    enabled
    reconciliationFailed
    port
    deployments {
      id
    }
  }
}
