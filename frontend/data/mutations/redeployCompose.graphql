mutation redeployCompose($id: Uuid!) {
  redeployCompose(id: $id) {
    id
    name
    current {
      id
      directory
      serviceEnabled
      serviceName
      content
    }
  }
}
