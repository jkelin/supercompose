subscription onConnectionLog($deploymentId: Uuid, $after: DateTime!) {
  connectionLogs: onConnectionLog(deploymentId: $deploymentId, after: $after) {
    id
    error
    message
    severity
    time
  }
}
