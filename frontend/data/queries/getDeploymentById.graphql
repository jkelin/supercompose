query getDeploymentById($id: Uuid!) {
  deployment(where: { id: { eq: $id } }) {
    id
    enabled
    compose {
      id
      name
    }
    node {
      id
      name
      host
      port
      username
    }
  }
}
